<%= form_for [:admin, @project] do |f| %>
  <p>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </p>
  <%= f.submit %> or <%= link_to "Cancel", admin_projects_path %>

<% end %>
